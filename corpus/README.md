# Test corpus

## `fixtures/`

Synthetic EPUBs generated by `scripts/build-fixtures.mjs` and committed. They encode
the *structures* that break real readers — books with no spread metadata, explicit
`page-spread-*` properties, RTL progression, mixed viewports, pre-composed spread
pages, missing viewports, and media overlays — without shipping anyone's
copyrighted artwork.

`narrated-spreads.epub` carries word-level media overlays over generated audio: a
tone per word, so the clip boundaries in the SMIL line up with something audible
rather than with silence. Narration covers pages 1-5 of 7, which leaves the last
spread deliberately silent — read-along has to carry over a page turn while there
is more to read, and stop when there is not, and those are different endings.

Regenerating rewrites every fixture, because the zip records a modification time.
Only commit the ones whose *contents* changed.

## `local/`

**Gitignored.** Real, in-copyright books used during development. The reference
book is `amelia-bedelia-birds.epub` (HarperCollins, ISBN 9780062334268), which is
the canonical target for fixed-layout + word-level read-along.

Tests that need it skip themselves when it is absent, so CI stays green without it.
